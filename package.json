{
  "name": "chatbot-widget",
  "version": "1.0.0",
  "description": "Embeddable chatbot widget — vanilla JS, zero dependencies, single script tag",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
